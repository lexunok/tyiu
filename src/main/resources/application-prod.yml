spring:
  datasource:
    username: admin
    password: admin
    url: jdbc:postgresql://postgres:5432/tyiu
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
