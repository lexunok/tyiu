postgres:
    url: postgresql://localhost:5433/hits
server:
    port: 7777
spring:
    datasource:
        url: jdbc:${postgres.url}
        username: admin
        password: admin
    jpa:
        show-sql: true
        properties:
          hibernate:
              dialect: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: update

